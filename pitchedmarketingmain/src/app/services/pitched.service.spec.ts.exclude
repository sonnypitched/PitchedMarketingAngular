import { TestBed, inject } from '@angular/core/testing';

import { PitchedService } from './pitched.service';

describe('PitchedService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PitchedService]
    });
  });

  it('should be created', inject([PitchedService], (service: PitchedService) => {
    expect(service).toBeTruthy();
  }));
});
